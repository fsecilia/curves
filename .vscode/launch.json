{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug cmake target",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [
                "--restore-config"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        }
    ]
}