{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Curves",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [
                "/home/frank/mouse.capture",
                "4000"
            ],
            "env": {
                "GTEST_COLOR": "1"
            },
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "all + kbuild"
        }
    ]
}