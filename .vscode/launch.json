{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Curves",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "environment": [
                {
                    "name": "GTEST_COLOR",
                    "value": "1"
                }
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "all + kbuild"
        }
    ]
}