{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Curves",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "env": {
                "GTEST_COLOR": "1"
            },
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "all + kbuild"
        }
    ]
}