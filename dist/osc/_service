<services>
  <service name="tar_scm">
    <param name="scm">git</param>
    <param name="url">https://github.com/fsecilia/curves.git</param>
    <param name="revision">main</param>
    <param name="filename">curves-mouse-acceleration</param>
    <param name="versionformat">@PARENT_TAG@</param>
    <param name="versionrewrite-pattern">v(.*)</param>
    <param name="versionrewrite-replacement">\1</param>
    <param name="changesgenerate">enable</param>
  </service>

  <service name="set_version">
    <param name="file">curves-mouse-acceleration.spec</param>
    <param name="file">curves-mouse-acceleration.dsc</param>
    <param name="file">debian.changelog</param>
  </service>

  <service name="recompress">
    <param name="compression">gz</param>
    <param name="file">*.tar</param>
  </service>
</services>
